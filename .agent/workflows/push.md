---
description: Push code to the correct remote repository
---

# Push Workflow

**IMPORTANT**: Always push to `origin` which points to `codehava/WIja-ogi.git`. Do NOT push to `old-origin` (IvanFan-Lab).

## Steps

// turbo
1. Check current branch:
```bash
cd "/Users/ivanfan/Projects/WIJA 3" && git branch --show-current
```

// turbo
2. Push to origin (codehava/WIja-ogi):
```bash
cd "/Users/ivanfan/Projects/WIJA 3" && git push origin main
```

## Remote Reference
- `origin` → `https://github.com/codehava/WIja-ogi.git` ✅ (USE THIS)
- `old-origin` → `https://github.com/IvanFan-Lab/WIja.git` ❌ (DO NOT USE)
